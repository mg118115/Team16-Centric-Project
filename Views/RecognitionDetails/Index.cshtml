@model IEnumerable<Team_16_Centric_Project.Models.RecognitionDetail>

@{
    ViewBag.Title = "Leaderboard";
}

<h2>Leaderboard</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeRecognition.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Recognition.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numberRecognitions)
        </th>
       
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeRecognition.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Recognition.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numberRecognitions)
        </td>
       
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RecognitionDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RecognitionDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RecognitionDetailID })
        </td>
    </tr>
}

</table>
